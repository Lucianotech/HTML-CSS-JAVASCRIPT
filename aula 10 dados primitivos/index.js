
const nome = 'Luiz'; // String
const numero = 10; // number
let nomeAluno; // Undefined -> não aponta para local nenhum na memória
let sobrenomeAluno = null; //Nulo -> Não aponta para local nenhum na memória
const boolean = true; // true ou false

let contador = 1; // Operador de incremento
contador++;
console.log(contador);


let contador1 = 10; // Operador de decremento
contador1--;
console.log(contador1);

let passo = 2; // Incremento na quantidade que você desejar pular
let contador2 = contador2 + passo;
console.log(contador2)

let contador3 =0;
contador3 += 2; //Incremento na quantidade que você des3jar pular
contador3 *= 2; // Pode ser colocado qualquer operador, basta colocar dentro da sua necessidade.
console.log(contador3);


// NaN -> Not a number
// (parseInt) -> converte string em inteiro
// (parseFloat) -> converte string em real
// (Number) -> ele conerte a string no formato automático
